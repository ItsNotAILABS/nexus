{
  "schema": "nexus.ecosystem-protocol-registry.v1",
  "version": "1.2.0-alpha.1",
  "authority": "ItsNotAILABS/nexus",
  "principles": [
    "provider-neutral",
    "tenant-aware",
    "receipt-backed",
    "bounded-execution",
    "capability-discovered",
    "truth-before-claims",
    "secret-values-never-on-wire",
    "human-approval-for-irreversible-actions",
    "idempotent-mutations",
    "failure-isolated-by-default"
  ],
  "protocols": [
    {"id":"nexus.capability.v1","purpose":"Discover component role, actions, limits, dependencies and proof state","required":["schema","component","role","actions","limits","proof"]},
    {"id":"nexus.identity-ref.v1","purpose":"Carry a stable principal and tenant reference without copying authentication secrets","required":["schema","principal_id","principal_type","tenant_id","auth_context"]},
    {"id":"nexus.task.v1","purpose":"Route bounded work between ecosystem components","required":["schema","request_id","action","principal","scope","budget","payload","issued_at"]},
    {"id":"nexus.plan.v1","purpose":"Compile multi-component work into explicit ordered steps, dependencies, budgets and acceptance gates","required":["schema","plan_id","request_id","steps","acceptance","created_at"]},
    {"id":"nexus.job.v1","purpose":"Represent asynchronous or long-running work with explicit lifecycle, owner, progress, cancellation and result references","required":["schema","job_id","request_id","component","action","status","created_at","updated_at"]},
    {"id":"nexus.policy-decision.v1","purpose":"Return allow, deny or confirm decisions before governed action","required":["schema","request_id","decision","policy_id","reasons","decided_at"]},
    {"id":"nexus.approval.v1","purpose":"Record an explicit human or delegated approval decision for a bounded irreversible or privileged action","required":["schema","approval_id","request_id","actor","decision","scope","created_at","expires_at"]},
    {"id":"nexus.denial.v1","purpose":"Machine-readable refusal or denial receipt for unsafe, unsupported or unauthorized action","required":["schema","request_id","component","code","message","policy_id","created_at"]},
    {"id":"nexus.budget.v1","purpose":"Bound time, tokens, cost, files, bytes, subprocesses, child agents and external calls","required":["schema","limits"]},
    {"id":"nexus.quota.v1","purpose":"Represent tenant, API-key, device or principal quotas and current usage over a defined window","required":["schema","subject","window","limits","usage","observed_at"]},
    {"id":"nexus.idempotency.v1","purpose":"Deduplicate mutating operations using a scoped client key, request digest and cached outcome reference","required":["schema","key","scope","request_digest","state","created_at","expires_at"]},
    {"id":"nexus.lease.v1","purpose":"Coordinate temporary exclusive ownership of a job, resource or artifact lane with expiry and renewal semantics","required":["schema","lease_id","resource","holder","acquired_at","expires_at","state"]},
    {"id":"nexus.retry-policy.v1","purpose":"Bound automatic retries with attempt limits, backoff, retryable codes and terminal behavior","required":["schema","max_attempts","backoff","retryable_codes","terminal_action"]},
    {"id":"nexus.circuit-breaker.v1","purpose":"Protect degraded dependencies by tracking failures, open intervals, recovery probes and fallback routing","required":["schema","dependency","state","failure_count","threshold","updated_at"]},
    {"id":"nexus.event.v1","purpose":"Publish a typed domain event with tenant scope, correlation, ordering key and payload reference","required":["schema","event_id","type","source","scope","correlation_id","payload","occurred_at"]},
    {"id":"nexus.feature-flag.v1","purpose":"Control staged rollout by tenant, principal, environment or percentage without changing protocol contracts","required":["schema","flag","state","audience","updated_at"]},
    {"id":"nexus.incident.v1","purpose":"Track a reliability or security incident through detection, containment, recovery, evidence and closure","required":["schema","incident_id","severity","status","components","detected_at","timeline"]},
    {"id":"nexus.secret-ref.v1","purpose":"Reference an operator-managed secret binding by provider, scope and purpose without transporting the secret value","required":["schema","provider","ref","purpose","scope"]},
    {"id":"nexus.retention-policy.v1","purpose":"Define sensitivity-aware retention, deletion and legal-hold behavior for stored ecosystem data","required":["schema","data_class","retention","deletion","legal_hold"]},
    {"id":"nexus.context-pack.v1","purpose":"Move curated context without copying entire private state","required":["schema","request_id","items","provenance"]},
    {"id":"nexus.memory-event.v1","purpose":"Record durable outcome or lesson events with provenance, sensitivity and retention class","required":["schema","event_id","subject","kind","content","provenance","created_at"]},
    {"id":"nexus.artifact.v1","purpose":"Describe produced files, data, models or reports with hashes, media types and lineage","required":["schema","artifact_id","kind","sha256","created_by","created_at"]},
    {"id":"nexus.execution-receipt.v1","purpose":"Record bounded execution outcomes without private model reasoning","required":["schema","request_id","status","component","started_at","finished_at","digest"]},
    {"id":"nexus.audit-event.v1","purpose":"Record actor, action, target, policy and outcome correlation for privileged or externally visible events","required":["schema","event_id","request_id","actor","component","action","outcome","observed_at"]},
    {"id":"nexus.health.v1","purpose":"Normalize liveness, readiness, degradation and dependency state","required":["schema","component","status","checks","observed_at"]},
    {"id":"nexus.telemetry.v1","purpose":"Emit privacy-bounded metrics and traces with request correlation","required":["schema","component","request_id","metrics","observed_at"]},
    {"id":"nexus.compatibility.v1","purpose":"Declare protocol versions produced and consumed plus minimum compatible versions","required":["schema","component","produces","consumes","minimums"]},
    {"id":"nexus.handoff.v1","purpose":"Transfer responsibility between components with explicit next owner and evidence","required":["schema","request_id","from","to","reason","artifacts","created_at"]},
    {"id":"nexus.release-evidence.v1","purpose":"Describe source, test, package, preview and deploy evidence for a release claim","required":["schema","component","version","evidence","truth_state","created_at"]}
  ]
}
